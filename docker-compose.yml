services:
  app:
    build:
      context: .
      target: development
    command: npm run dev-all
    ports:
      - "3000:3000"
      - "3001:3001"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    stdin_open: true
    tty: true
